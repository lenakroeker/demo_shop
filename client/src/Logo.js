import React from "react";
import styled from "styled-components";

export const Logo = () => {
  return <Wrapper>Demo Shop</Wrapper>;
};

const Wrapper = styled.div`
  box-sizing: border-box;
  display: inline;
  font-family: "Aladin", cursive;
  color: lavender;
  font-size: 30px;
`;

export default Logo;
